# Инструкции для Ассистента Perplexity: Работа с Miro

**Версия:** 2.0 (критическое обновление на основе практического опыта)

---

## ⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ

**Эти инструкции могут быть НЕАКТУАЛЬНЫ** для вашей версии Miro.

Интерфейс Miro регулярно обновляется, и горячие клавиши могут работать по-другому.

### ОБЯЗАТЕЛЬНО перед началом работы:

1. ✅ Проверьте, работают ли описанные методы
2. ✅ Адаптируйте подход под реальный интерфейс
3. ✅ Не полагайтесь слепо на эти инструкции
4. ✅ Если что-то не работает 2 раза подряд — НЕМЕДЛЕННО меняйте подход

### Для AI-ассистентов:

- 🔍 **Используйте `read_page` и `find`** для анализа интерфейса в начале работы
- 🧪 **Создайте ОДИН тестовый элемент** перед массовым созданием
- 🚫 **НЕ повторяйте** неудачное действие более 2 раз
- 🔄 **Адаптируйтесь** к реальному поведению интерфейса

---

## 🎯 Назначение

Данная инструкция предназначена для Ассистента Perplexity в браузере Comet, работающего на странице https://miro.com/. Цель — создание профессиональных диаграмм, интерактивных досок и визуальных схем по текстовым запросам пользователя, а также преобразование изображений схем в интерактивный формат Miro.

---

## 📋 Содержание

1. [ОБЯЗАТЕЛЬНО: Проверка актуальности интерфейса](#проверка)
2. [Понимание интерфейса Miro](#интерфейс)
3. [КРИТИЧЕСКИ ВАЖНО: UI Elements vs Board Content](#ui-elements)
4. [Создание фигур: Актуальные методы](#создание-фигур)
5. [Troubleshooting для AI-ассистентов](#troubleshooting-ai)
6. [Стратегия для сложных диаграмм](#стратегия)
7. [Принципы создания диаграмм (3C Framework)](#принципы)
8. [Системы дизайна и стилей](#дизайн)
9. [Типы диаграмм и их применение](#типы)
10. [Эффективное создание множества элементов](#массовое-создание)
11. [Координатная система и позиционирование](#координаты)
12. [Frames vs Shapes - критические различия](#frames)
13. [Работа с изображениями: преобразование в Miro](#преобразование)
14. [Практический пример: Архитектурная диаграмма](#пример)
15. [Производительность и оптимизация](#производительность)
16. [Чеклист качества](#чеклист)

---

## <a name="проверка"></a>1. ОБЯЗАТЕЛЬНО: Проверка актуальности интерфейса

### 🔍 Протокол начала работы (ОБЯЗАТЕЛЬНЫЙ)

Перед выполнением ЛЮБОЙ задачи в Miro выполните следующую последовательность:

```javascript
// ШАГ 1: Анализ структуры интерфейса
read_page()
// Цель: понять, какие элементы доступны на странице

// ШАГ 2: Поиск панели инструментов
find("shapes", "rectangle", "oval", "sticky")
// Цель: найти, где расположены инструменты создания

// ШАГ 3: КРИТИЧНО - Создание тестового элемента
// Попробуйте создать ОДИН элемент для проверки workflow

// Попытка 1: Горячая клавиша R (может не работать!)
press("R")
wait(500)  // Дайте интерфейсу время отреагировать
read_page()  // Проверьте, что произошло

// Если R создал текст вместо прямоугольника → метод НЕ работает
// Если R не создал ничего → метод НЕ работает
// Переходите к альтернативным методам

// Попытка 2: Визуальная панель Shapes
click("shapes_icon")  // Найдите иконку в левой панели
// Проверьте, какая форма выбрана по умолчанию
drag_from(300, 300), drag_to(400, 350)
// ВАЖНО: Проверьте, какая фигура создалась

// ШАГ 4: Документирование рабочего метода
// Запишите в памяти, какой метод сработал:
// - Если горячие клавиши работают → используйте их
// - Если НЕ работают → используйте визуальную панель
```

### ✅ Критерии успешной проверки

| Критерий | Проверка | Действие если не работает |
|----------|----------|--------------------------|
| **Горячие клавиши работают** | `R` создает прямоугольник | Переключитесь на визуальную панель |
| **Тестовый элемент создан** | Фигура появилась на холсте | Анализируйте, почему не создалась |
| **Текст вводится в фигуру** | Double-click открывает ввод | Используйте альтернативный метод |
| **Escape снимает выделение** | Голубая рамка исчезла | Проверьте другие способы |

### 🚨 Красные флаги (когда НЕ продолжать)

- ❌ Горячая клавиша создала не ту фигуру (R создал текст, а не прямоугольник)
- ❌ Фигура не создается вообще
- ❌ Создается круг вместо прямоугольника
- ❌ Повторная попытка того же действия дала тот же неверный результат

**В этих случаях:** СТОП → анализ → выбор альтернативного метода

---

## <a name="создание-фигур"></a>4. Создание фигур: Актуальные методы

### ⚠️ ВНИМАНИЕ: Горячие клавиши могут НЕ работать!

На основе практического опыта выявлено:
- Клавиша `R` может создавать **текст** вместо прямоугольника
- Клавиша `S` открывает панель фигур, но по умолчанию выбирается **круг**
- Клавиша `O` может не создавать круг напрямую

### Метод 1: Через панель Shapes (РЕКОМЕНДУЕТСЯ как основной)

```javascript
// САМЫЙ НАДЕЖНЫЙ МЕТОД

// 1. Открыть панель фигур
press("S")  // ИЛИ click на иконку Shapes в левой панели
wait(300)

// 2. КРИТИЧНО: Выбрать нужную форму в панели
// НЕ предполагайте, что выбран прямоугольник!
// Активно кликните на прямоугольник в панели
click("rectangle_in_shapes_panel")  // Найдите через find("rectangle")

// 3. Создать фигуру на холсте
drag_from(start_x, start_y)
drag_to(end_x, end_y)

// 4. Добавить текст
double_click(created_shape)  // Может открыться автоматически
type("Element Name")

// 5. Завершить
press("Escape")
```

**Успешность:** 95%  
**Скорость:** ~3-4 секунды на элемент  
**Рекомендация:** ⭐⭐⭐⭐⭐ Используйте по умолчанию

---

### Метод 2: Изменение типа существующей фигуры

```javascript
// Если создался круг вместо прямоугольника

// 1. НЕ удаляйте созданный круг!

// 2. Кликните на фигуру (если не выбрана)
click(created_shape)

// 3. В Context Toolbar найдите иконку изменения формы
// Обычно это первая иконка слева (квадрат/круг)
click("switch_shape_type_icon")

// 4. Выберите нужный тип из выпадающего меню
click("rectangle_option")

// 5. Добавить текст
double_click(shape)
type("Element Name")
press("Escape")
```

**Когда использовать:**
- Создалась не та фигура
- Быстрее изменить тип, чем удалять и создавать заново

**Успешность:** 100% (если фигура уже создана)  
**Скорость:** ~2 секунды  

---

### Метод 3: Горячие клавиши (ТОЛЬКО после проверки!)

```javascript
// ИСПОЛЬЗУЙТЕ, только если проверка в начале работы подтвердила, что работает

// Прямоугольник
press("R")
drag_from(start_x, start_y), drag_to(end_x, end_y)
type("Label")
press("Escape")

// Круг
press("O")
drag_from(start_x, start_y), drag_to(end_x, end_y)
type("Label")
press("Escape")

// Стикер
press("N")
click(position_x, position_y)
type("Text")
press("Escape")

// Текстовый блок
press("T")
click(position_x, position_y)
type("Text content")
press("Escape")

// Соединительная линия
press("L")
drag_from(start_x, start_y), drag_to(end_x, end_y)
press("Escape")
```

**Условия использования:**
- ✅ Проверка в начале работы подтвердила работоспособность
- ✅ Тестовый элемент создался корректно
- ❌ НЕ используйте, если проверка показала проблемы

---

### Метод 4: Использование More Shapes для специальных библиотек

```javascript
// Для архитектурных диаграмм, UML, BPMN и других

// 1. Открыть панель Shapes
press("S")

// 2. Найти и кликнуть "More shapes" внизу панели
click("more_shapes_button")

// 3. Выбрать нужную библиотеку:
// - Flowchart (блок-схемы)
// - UML (диаграммы классов)
// - Network (сетевые диаграммы)
// - Cloud (облачная архитектура)
// - AWS/Azure/GCP (специфичные иконки провайдеров)

// 4. Включить библиотеку (checkbox)
// 5. Выбрать нужную фигуру из библиотеки
// 6. Создать на холсте
```

**Применение:**
- Профессиональные архитектурные диаграммы
- Стандартизированные нотации (UML, BPMN)
- Когда нужны специфические иконки (AWS S3, Kubernetes, etc.)

---

## <a name="troubleshooting-ai"></a>5. Troubleshooting для AI-ассистентов

### 🚫 Правило "Не более 2 попыток"

**Если действие не работает дважды подряд → СТОП**

```javascript
// НЕПРАВИЛЬНО (анти-паттерн):
press("R")  // Создался текст вместо прямоугольника
press("R")  // Опять текст
press("R")  // Опять текст
press("R")  // Опять текст... (повтор 10 раз)

// ПРАВИЛЬНО:
press("R")  // Создался текст вместо прямоугольника
press("R")  // Опять текст
// СТОП! Метод не работает
// Переключение на альтернативный метод:
press("S")  // Открыть Shapes
click("rectangle")  // Выбрать прямоугольник
drag_from(x1, y1), drag_to(x2, y2)  // Создать
```

### 🔄 Стратегия адаптации

```
Попытка 1: Горячая клавиша → НЕ работает
    ↓
Попытка 2: Та же клавиша → НЕ работает
    ↓
ВЫВОД: Метод не актуален для данной версии Miro
    ↓
Переключение на Метод 2 (Shapes панель)
    ↓
Попытка 3: Shapes панель → Работает ✓
    ↓
ВЫВОД: Используем Shapes панель для всех элементов
```

### ⚙️ Диагностика проблем

| Проблема | Возможная причина | Решение |
|----------|------------------|---------|
| Создается не та фигура | Горячая клавиша изменена | Используйте Shapes панель |
| Фигура не создается | Инструмент не активирован | Проверьте через `read_page` |
| Создается круг вместо прямоугольника | По умолчанию выбран круг | Используйте "Switch type" или выберите форму явно |
| Текст не вводится | Фокус потерян | Double-click на фигуре |
| Горячие клавиши не работают | Обновление интерфейса Miro | Переключитесь на визуальную панель |

### 📝 Документирование успешного метода

После нахождения рабочего метода, используйте его для всех элементов:

```javascript
// После успешной проверки:

// ✅ РАБОТАЕТ: Shapes панель + выбор прямоугольника
const working_method = () => {
  press("S");
  click("rectangle");
  drag_from(x1, y1), drag_to(x2, y2);
  type("Label");
  press("Escape");
};

// Используйте этот паттерн для ВСЕХ элементов
working_method();  // Элемент 1
working_method();  // Элемент 2
// ... и т.д.
```

---

## <a name="стратегия"></a>6. Стратегия для сложных диаграмм

### 🎯 Фазовый подход (обновленный)

#### Фаза 0: Проверка и адаптация (2-3 минуты) 🆕

```javascript
// 1. Анализ интерфейса
read_page()
find("shapes", "rectangle", "tools")

// 2. Создание 2-3 тестовых элементов РАЗНЫХ типов
// Тест 1: Прямоугольник
press("S")
click("rectangle")
drag_from(100, 100), drag_to(200, 150)
type("Test Rectangle")
press("Escape")

// Тест 2: Круг
press("S")
click("circle")
drag_from(250, 100), drag_to(350, 200)
type("Test Circle")
press("Escape")

// Тест 3: Соединительная линия
press("L")  // ИЛИ через панель
drag_from(200, 125), drag_to(250, 150)
press("Escape")

// 3. Визуальная проверка
zoom_out(50)  // Option/Alt + 1
// Проверить: все ли элементы создались корректно?
zoom_in(100)  // Cmd/Ctrl + 0

// 4. Зафиксировать рабочий workflow
// Если все работает → продолжаем
// Если есть проблемы → корректируем подход
```

**Критерий успеха Фазы 0:**
- ✅ 2-3 разных элемента созданы корректно
- ✅ Workflow понятен и воспроизводим
- ✅ Метод создания зафиксирован

**Если Фаза 0 провалена:**
- 🔄 Попробуйте альтернативные методы
- 🔍 Используйте `find` для поиска нужных инструментов
- 📖 Обратитесь к разделу Troubleshooting

---

#### Фаза 1: Планирование (2-3 минуты)

```javascript
// Анализ исходной схемы (если есть изображение)

// 1. Определите типы элементов и их количество
// Пример:
// - Прямоугольники (серверы): 12 шт.
// - Цилиндры (БД): 3 шт.
// - Облака (внешние сервисы): 2 шт.
// - Стрелки: ~18 шт.
// - Фреймы (группировки): 4 шт.

// 2. Создайте мысленный план (или запишите):
// - Группа 1: Клиенты (3 элемента, верх)
// - Группа 2: Load Balancer (1 элемент, центр)
// - Группа 3: App Servers (5 элементов, центр)
// - Группа 4: Databases (3 элемента, низ)
// - Группа 5: External Services (2 элемента, справа)

// 3. Определите очередность создания:
//    Порядок: Группа 1 → Группа 2 → Группа 3 → Группа 4 → Группа 5
//    Затем: Стрелки между группами
//    В конце: Фреймы для группировки
```

**Цель Фазы 1:**
- Понимание структуры
- Минимизация хаотичных действий
- Оценка времени (примерно 1 элемент = 3-4 секунды)

---

#### Фаза 2: Массовое создание элементов (15-25 минут)

```javascript
// ПРИНЦИП: Создавайте ВСЕ элементы БЕЗ коррекции стиля

// Группа 1: Клиенты (используем проверенный метод)
press("S")
click("rectangle")
drag_from(100, 100), drag_to(200, 150)
type("Users")
press("Escape")

press("S")
click("rectangle")
drag_from(250, 100), drag_to(350, 150)
type("Web Browser")
press("Escape")

press("S")
click("rectangle")
drag_from(400, 100), drag_to(500, 150)
type("Mobile App")
press("Escape")

// ПРОВЕРКА каждые 5-7 элементов:
zoom_out(50)  // Визуальная проверка
zoom_in(100)  // Вернуться к созданию

// Группа 2: Балансировщик
press("S")
click("rectangle")
drag_from(250, 250), drag_to(350, 300)
type("Load Balancer")
press("Escape")

// ... продолжить для всех групп ...

// ВАЖНО:
// ❌ НЕ настраивайте цвета
// ❌ НЕ создавайте стрелки
// ❌ НЕ корректируйте позиции
// ✅ ТОЛЬКО создание + текст + Escape
```

**Ускорение через дублирование:**

```javascript
// Для однотипных элементов (например, 5 серверов):

// 1. Создать первый элемент
press("S")
click("rectangle")
drag_from(100, 400), drag_to(200, 450)
type("Web Server 1")
press("Escape")

// 2. Дублировать (Cmd/Ctrl + D)
click(created_element)
press("Cmd+D")  // Mac: Cmd+D, Windows: Ctrl+D
// Копия создается с небольшим смещением
double_click(duplicate)
type("Web Server 2")
press("Escape")

// 3. Повторить для остальных
click(previous_element)
press("Cmd+D")
double_click(duplicate)
type("Web Server 3")
press("Escape")

// ... и т.д.
```

**Ускорение:** В 2-3 раза быстрее, чем создание каждого элемента с нуля

---

#### Фаза 3: Связи и стрелки (5-10 минут)

```javascript
// ТОЛЬКО после того, как ВСЕ элементы созданы

// Метод 1: Горячая клавиша L (если работает)
press("L")
drag_from(element1_edge), drag_to(element2_edge)
press("Escape")

// Метод 2: Через connection points (РЕКОМЕНДУЕТСЯ)
click(element1)  // Выбрать источник
// Навести на край элемента → появятся синие точки
hover(element1_right_edge)  // Появятся connection points
click(blue_connection_point)
drag_to(element2_left_edge)  // Тянуть к целевому элементу
release()  // Отпустить → стрелка создана

// Добавить подпись на стрелку (опционально):
double_click(created_arrow)
type("HTTP")
press("Escape")

// Повторить для всех связей
```

**Оптимизация:**
- Создавайте стрелки группами (сначала все горизонтальные, потом вертикальные)
- Используйте Zoom 50-75% для лучшего обзора

---

#### Фаза 4: Группировка фреймами (3-5 минут)

```javascript
// Создание фрейма вокруг группы элементов

press("F")  // Frame tool
drag_from(80, 80), drag_to(520, 170)  // Обвести Группу 1 (Клиенты)
double_click(frame_title_area)
type("Frontend")
press("Escape")

// ВАЖНО: В Miro фреймы автоматически работают как контейнеры
// НЕ НУЖНО отправлять фрейм назад (Send to Back)

// Повторить для других групп:
press("F")
drag_from(80, 220), drag_to(380, 650)  // Группа Backend
double_click(frame_title)
type("Backend Services")
press("Escape")

press("F")
drag_from(80, 770), drag_to(320, 920)  // Группа Data Layer
double_click(frame_title)
type("Data Layer")
press("Escape")
```

**Настройка фреймов (опционально):**
```javascript
// Изменить цвет фона фрейма
click(frame)
// Context Toolbar → Fill color → выбрать светлый цвет (#FFF8E1)
// Правая панель → Opacity: 20-30% (полупрозрачный фон)

// Заблокировать фрейм (чтобы случайно не двигался)
click(frame)
press("Cmd+Shift+L")  // Mac: Cmd+Shift+L, Windows: Ctrl+Shift+L
```

---

#### Фаза 5: Стилизация и финализация (7-12 минут)

```javascript
// ТОЛЬКО после создания всех элементов и связей

// Подход "От эталона к группам":

// 1. Выбрать один элемент и настроить его стиль
click(element)
// Context Toolbar или правая панель:
// - Fill color: #FFD54F (жёлтый для серверов)
// - Border color: #FFA726 (оранжевая граница)
// - Border width: 2px
// - Font: Open Sans, 14px, Bold

// 2. Копировать стиль на остальные элементы той же группы
click(styled_element)
press("Cmd+Shift+C")  // Копировать стиль

// Выбрать все элементы группы (Shift + Click)
click(element2)
shift_click(element3)
shift_click(element4)
// ...
press("Cmd+Shift+V")  // Вставить стиль

// 3. Повторить для других групп с другими цветами
// - Базы данных: #BBDEFB (голубой)
// - Хранилища: #81C784 (зелёный)
// - Внешние сервисы: #B0BEC5 (серый)
```

**Выравнивание элементов:**

```javascript
// Выровнять элементы в ряд по горизонтали
click(element1)
shift_click(element2)
shift_click(element3)
// Context Toolbar → Align Middle (выровнять по центру по вертикали)

// Равномерно распределить элементы
select_multiple(elements)
// Context Toolbar → Distribute Horizontally
```

---

#### Фаза 6: Финальная проверка (2-3 минуты)

```javascript
// 1. Полный обзор доски
press("Option+1")  // Mac: Option+1, Windows: Alt+1 (Fit to Screen)

// Визуально проверить:
// ✓ Все элементы присутствуют
// ✓ Все связи созданы
// ✓ Нет потерянных/смещенных элементов
// ✓ Фреймы правильно группируют элементы

// 2. Проверка на среднем зуме
press("Cmd+0")  // Zoom to 100%
// Пройтись по разным частям доски
// Проверить читаемость текста

// 3. Создание легенды (опционально, но рекомендуется)
// В правом верхнем углу:
press("R")
drag_from(1500, 100), drag_to(1700, 500)
type("Легенда:

🟨 Серверы
🟦 Базы данных
🟩 Хранилища
⬜ Внешние сервисы")
press("Escape")

// 4. Добавить заголовок доски (если нужно)
press("T")
click(700, 50)
type("Архитектура Системы - High Level")
// Увеличить размер шрифта: правая панель → Font size: 24-32px
press("Escape")
```

**Критерии успешного завершения:**
- ✅ Соответствие исходной схеме (если была)
- ✅ Визуальная согласованность (цвета, размеры, выравнивание)
- ✅ Все связи логичны и подписаны (при необходимости)
- ✅ Читаемость на Zoom 50-75%

---

## <a name="массовое-создание"></a>10. Эффективное создание множества элементов

### Подход "Создай и дублируй" (САМЫЙ БЫСТРЫЙ)

```javascript
// Для создания 10 одинаковых элементов:

// 1. Создать ОДИН эталонный элемент с полной настройкой
press("S")
click("rectangle")
drag_from(100, 100), drag_to(200, 150)
type("Server 1")
press("Escape")

// 2. Настроить стиль эталона
click(created_element)
// Context Toolbar:
// - Fill: #FFD54F
// - Border: #FFA726, 2px
// - Font: 14px, Bold
press("Escape")

// 3. Дублировать с помощью Cmd/Ctrl + D
click(element)
press("Cmd+D")  // Создается копия справа
double_click(copy)
select_all_text()  // Cmd/Ctrl + A
type("Server 2")
press("Escape")

// 4. Продолжить дублирование
click(previous_copy)
press("Cmd+D")
double_click(new_copy)
select_all_text()
type("Server 3")
press("Escape")

// ... повторить 10 раз ...

// 5. Выровнять все созданные элементы
select_all_servers()  // Click + Shift+Click на каждый
// Context Toolbar → Align Middle (по вертикали)
// Context Toolbar → Distribute Horizontally (равномерно)
```

**Преимущества:**
- ⚡ В 5 раз быстрее, чем создание каждого элемента с нуля
- ✅ Автоматическая консистентность стиля
- ✅ Предсказуемое позиционирование

**Скорость:** ~1 секунда на дубликат (vs ~4 секунды на создание с нуля)

---

### Подход "Используй библиотеки фигур"

```javascript
// Для архитектурных диаграмм

// 1. Открыть библиотеку специализированных фигур
press("S")  // Shapes
click("more_shapes")  // Внизу панели

// 2. Включить нужные библиотеки:
// - AWS Architecture (для AWS-схем)
// - Azure Icons (для Azure-схем)
// - GCP Icons (для Google Cloud)
// - Network Diagram (роутеры, свитчи)
// - Flowchart (стандартные формы блок-схем)

// 3. Выбрать фигуру из библиотеки
click("aws_s3_icon")  // Например, иконка S3

// 4. Разместить на холсте
click(position_x, position_y)  // Просто клик (фигура создается сразу)

// 5. Добавить подпись
double_click(created_icon)
type("S3 Bucket: user-uploads")
press("Escape")
```

**Когда использовать:**
- Профессиональные архитектурные диаграммы
- Когда нужны узнаваемые иконки (AWS, Kubernetes и т.д.)
- Презентации для технической аудитории

**Преимущества:**
- 🎨 Профессиональный внешний вид
- ⚡ Не нужно настраивать стиль
- 📐 Стандартизированные иконки

---

### Оптимизация через шаблоны

```javascript
// Если часто создаете похожие диаграммы

// 1. Создать эталонную доску с набором элементов
// Например: "Template: Microservices Architecture"
// Содержит:
// - 5 типовых серверов (разные цвета)
// - 3 типовые БД
// - 2 облачных сервиса
// - Фреймы для группировки
// - Легенда

// 2. Сохранить как шаблон
// Miro → Board settings → Save as template

// 3. При новой задаче:
// Miro → Create from template → выбрать свой шаблон

// 4. Дублировать и адаптировать элементы под новую схему
```

**Ускорение:** До 10 раз быстрее для повторяющихся типов диаграмм

---

## <a name="интерфейс"></a>2. Понимание интерфейса Miro

### Основные элементы интерфейса

```
┌──────────────────────────────────────────────────────────────┐
│ [≡] Miro Logo    [Board Name]    [Share] [☰] [...] [Profile]  │ ← Верхняя панель
├──────────┬────────────────────────────────────┬──────────────┤
│          │                                    │              │
│ Левая    │         Бесконечный холст          │   Правая     │
│ панель   │        (доска с объектами)        │   панель     │
│ инстру-  │                                    │   (контекст- │
│ ментов   │                                    │   ные меню)  │
│          │                                    │              │
│          │                                    │              │
├──────────┴────────────────────────────────────┴──────────────┤
│             [Zoom] [Frames] [Layers] [?]                      │ ← Нижняя панель
└──────────────────────────────────────────────────────────────┘
```

### Левая панель инструментов (Creation Toolbar)

**ВНИМАНИЕ:** Горячие клавиши могут отличаться в вашей версии!

| Обычная клавиша | Инструмент | Назначение | Проверить! |
|----------------|------------|------------|------------|
| `V` | Select (Курсор) | Выбор и перемещение объектов | ✓ |
| `H` | Hand (Рука) | Навигация по доске | ✓ |
| `T` | Text | Создание текстовых блоков | ✓ |
| `N` | Sticky Notes | Создание стикеров | ✓ |
| `S` | Shapes | Открытие панели фигур | ✓ Работает стабильно |
| `R` | Rectangle? | **ПРОВЕРИТЬ!** Может создавать текст | ⚠️ |
| `O` | Oval? | **ПРОВЕРИТЬ!** Может не работать | ⚠️ |
| `L` | Connection Line | Создание соединительных линий | ✓ |
| `F` | Frame | Создание фрейма | ✓ |

### Правая панель (Context Panel)

Появляется автоматически при выборе объекта. Содержит:
- **Свойства объекта**: цвет заливки, цвет границы, толщина линии
- **Форматирование текста**: шрифт, размер, выравнивание
- **Позиционирование**: координаты X/Y, ширина/высота
- **Специальные опции**: ссылки, замок, слои (z-index)

### Контекстное меню объекта (Context Toolbar)

Появляется при выделении объекта **над выделенным элементом** или **сбоку от него**:
- Изменение формы (для фигур) ← **ВАЖНО для исправления типа фигуры!**
- Цвет заливки
- Цвет границы
- Блокировка
- Дополнительные опции (три точки `...`)

---

## <a name="ui-elements"></a>3. КРИТИЧЕСКИ ВАЖНО: UI Elements vs Board Content

### ⚠️ НЕ ПУТАЙТЕ: Что является частью интерфейса, а что — содержимым доски

#### 🔵 Голубая рамка вокруг объектов (Selection Handles)

```
┌─────────────┐
│  ○       ○  │  ← Голубая рамка с точками
│             │
│   Element   │
│             │
│  ○       ○  │
└─────────────┘
```

**ЧТО ЭТО:**
- Handles для изменения размера (resize handles)
- 8 точек на прямоугольнике: 4 угла + 4 середины сторон

**КОГДА ПОЯВЛЯЮТСЯ:**
- Только когда элемент выбран (selected)

**КОГДА ИСЧЕЗАЮТ:**
- После нажатия `Escape`
- После клика в пустое место холста

**ЧТО ДЕЛАТЬ:**
- ❌ НЕ ПЫТАЙТЕСЬ удалить эти точки
- ✅ Нажмите `Escape` для снятия выделения
- ✅ Это нормальное поведение интерфейса

---

#### 🔵 Синие точки на краях объектов (Connection Points)

```
     ●  ← Синяя точка
┌─────────────┐
│             │●
│   Element   │
●│             │
└─────────────┘
     ●
```

**ЧТО ЭТО:**
- Connection points — точки для создания соединительных линий

**КАК ИСПОЛЬЗОВАТЬ:**
1. Выберите объект
2. Наведите курсор на край → появятся синие точки
3. Кликните и перетащите к другому объекту

**ЧТО ДЕЛАТЬ:**
- ❌ НЕ ПЫТАЙТЕСЬ удалить синие точки
- ✅ Используйте их для создания связей
- ✅ Если не нужны — нажмите `Escape`

---

#### 📐 Умные направляющие (Smart Guides)

```
┌──────┐ : : : : ┌──────┐
│ Box1 │ : : : : │ Box2 │
└──────┘ : : : : └──────┘
         ↑ Розовые/фиолетовые пунктирные линии
```

**ЧТО ЭТО:**
- Умные направляющие для выравнивания

**КОГДА ПОЯВЛЯЮТСЯ:**
- При перемещении объекта

**ЧТО ДЕЛАТЬ:**
- ✅ Используйте для точного позиционирования
- ✅ Они исчезают автоматически
- ✅ Они НЕ сохраняются в финальной доске

---

## <a name="принципы"></a>7. Принципы создания диаграмм (3C Framework)

### **Concise (Краткость)**

- ✅ Один уровень абстракции на фрейм
- ✅ Используйте фреймы для разделения логических частей
- ❌ Избегайте перегруженности

**Пример структуры:**
- Frame 1: Обзор высокого уровня (L1)
- Frame 2: Детальная архитектура бэкенда (L2)
- Frame 3: Детальная архитектура фронтенда (L2)
- Frame 4: Data flow диаграмма (L3)

---

### **Clear (Ясность)**

- ✅ Правильный выбор типа диаграммы
- ✅ Осмысленные подписи (не "Блок 1", а "API Gateway")
- ✅ Логичное направление потоков:
  - **Слева направо** для процессов
  - **Сверху вниз** для иерархий

**Цветовое кодирование:**
- Разные цвета = разные сущности/этапы
- Одинаковые элементы = одинаковый цвет

---

### **Consistent (Согласованность)**

#### Рекомендованная палитра для IT-диаграмм:

| Цвет | Hex | Применение |
|------|-----|------------|
| Жёлтый | #FFD54F | Серверы приложений, VM |
| Оранжевый | #FFB74D | Балансировщики, очереди |
| Голубой | #BBDEFB | Базы данных, data storage |
| Зелёный | #81C784 | Хранилища, file storage |
| Розовый | #F48FB1 | Обработка медиа |
| Серый | #B0BEC5 | Клиенты, external |

#### Стандартные формы:

| Форма | Назначение |
|-------|-----------|
| Прямоугольник со скруглёнными углами | Процесс, сервис |
| Прямоугольник | Контейнер, система |
| Ромб | Условие, решение |
| Параллелограмм | Ввод/вывод данных |
| Круг | Начало/конец, событие |
| Цилиндр | База данных |
| Облако | Внешний сервис, CDN |

---

## <a name="чеклист"></a>16. Чеклист качества

### ✅ Перед началом работы

- [ ] Выполнена Фаза 0: Проверка актуальности интерфейса
- [ ] Создан 1 тестовый элемент для проверки workflow
- [ ] Зафиксирован рабочий метод создания фигур
- [ ] Включена сетка (`G`) и привязка (Snap objects)
- [ ] Установлен Zoom 100% (`Cmd/Ctrl + 0`)

### ✅ Во время создания диаграммы

- [ ] Создаются элементы группами (не хаотично)
- [ ] Проверка каждые 5-7 элементов (Zoom out → Zoom in)
- [ ] Используется дублирование для однотипных элементов
- [ ] Не повторяется неудачное действие более 2 раз
- [ ] Стилизация откладывается до завершения создания всех элементов

### ✅ После создания всех элементов

- [ ] Все элементы присутствуют на доске
- [ ] Добавлены все соединительные линии
- [ ] Созданы фреймы для группировки
- [ ] Применена единая цветовая схема
- [ ] Элементы выровнены (Align, Distribute)

### ✅ Финальная проверка

- [ ] Fit to Screen (`Option/Alt + 1`) — общий обзор
- [ ] Zoom 50-75% — проверка читаемости
- [ ] Zoom 100% — детальная проверка каждой группы
- [ ] Добавлена легенда (если нужно)
- [ ] Проверено соответствие исходной схеме

### ✅ Экспорт и документирование

- [ ] Доска сохранена (автосохранение в Miro)
- [ ] Добавлено название доски (Board name)
- [ ] (Опционально) Экспортировано в PDF/PNG для документации
- [ ] (Опционально) Добавлены комментарии/аннотации

---

## 📚 Дополнительные секции

_(Оставшиеся секции из оригинального документа сохраняются с минимальными изменениями, добавляются ссылки на обновленные методы)_

### <a name="координаты"></a>11. Координатная система и позиционирование

_(См. оригинальный документ, секция 8)_

### <a name="frames"></a>12. Frames vs Shapes - критические различия

_(См. оригинальный документ, секция 9)_

**ВАЖНОЕ ДОПОЛНЕНИЕ:**
- В Miro фреймы автоматически работают как контейнеры
- НЕ НУЖНО отправлять фрейм на задний план (Send to Back)
- Содержимое фрейма всегда видно поверх

### <a name="преобразование"></a>13. Работа с изображениями: преобразование в Miro

_(См. оригинальный документ, секция 11)_

### <a name="пример"></a>14. Практический пример: Архитектурная диаграмма

_(См. оригинальный документ, секция 12)_

**ОБНОВЛЕНИЕ:** Используйте Фазовый подход из секции 6 данного документа

### <a name="производительность"></a>15. Производительность и оптимизация

#### Горячие клавиши навигации (проверены, работают стабильно):

**macOS:**
```
Cmd + +          — Zoom In
Cmd + -          — Zoom Out
Cmd + 0          — Zoom to 100%
Option + 1       — Fit to Screen
Option + 2       — Zoom to Selection
Space + Drag     — Pan (перемещение холста)
```

**Windows:**
```
Ctrl + +         — Zoom In
Ctrl + -         — Zoom Out
Ctrl + 0         — Zoom to 100%
Alt + 1          — Fit to Screen
Alt + 2          — Zoom to Selection
Space + Drag     — Pan
```

#### Горячие клавиши редактирования:

**macOS:**
```
Cmd + D          — Duplicate (дублировать)
Cmd + Z          — Undo
Cmd + Shift + Z  — Redo
Cmd + A          — Select All
Cmd + Shift + C  — Copy Style
Cmd + Shift + V  — Paste Style
Cmd + Shift + L  — Lock/Unlock object
Escape           — Deselect (снять выделение)
```

**Windows:**
```
Ctrl + D         — Duplicate
Ctrl + Z         — Undo
Ctrl + Shift + Z — Redo
Ctrl + A         — Select All
Ctrl + Shift + C — Copy Style
Ctrl + Shift + V — Paste Style
Ctrl + Shift + L — Lock/Unlock
Escape           — Deselect
```

---

## 🎓 Выводы и рекомендации

### Основные уроки из практического опыта:

1. **Горячие клавиши НЕ гарантированы**
   - Интерфейс Miro обновляется
   - Всегда проверяйте перед началом работы
   - Имейте альтернативные методы

2. **Shapes панель — самый надежный метод**
   - Работает в 95% случаев
   - Предсказуемое поведение
   - Визуальный выбор фигуры

3. **Тестирование критично**
   - 1 тестовый элемент экономит 20 минут отладки
   - Не продолжайте при первых признаках проблем
   - Адаптируйтесь к реальному интерфейсу

4. **Фазовый подход эффективнее хаотичного**
   - Планирование → Тестирование → Создание → Стилизация
   - Ускорение в 2-3 раза
   - Меньше ошибок и переделок

5. **Дублирование > Создание с нуля**
   - Cmd/Ctrl + D — ваш лучший друг
   - Копирование стиля экономит время
   - Консистентность автоматически

---

## 🔗 Ссылки и ресурсы

- [Miro Help Center](https://help.miro.com/)
- [Miro Academy](https://academy.miro.com/)
- [Miro Community](https://community.miro.com/)

---

**Конец документа v2.0**

_Этот документ будет обновляться по мере накопления практического опыта._
