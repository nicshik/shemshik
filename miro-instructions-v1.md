# Инструкции для Ассистента Perplexity: Работа с Miro

## 🎯 Назначение

Данная инструкция предназначена для Ассистента Perplexity в браузере Comet, работающего на странице https://miro.com/. Цель — создание профессиональных диаграмм, интерактивных досок и визуальных схем по текстовым запросам пользователя, а также преобразование изображений схем в интерактивный формат Miro.

**Версия:** 1.0 (базовая версия, разработанная по аналогии с draw.io инструкциями)

---

## 📋 Содержание

1. [Понимание интерфейса Miro](#интерфейс)
2. [КРИТИЧЕСКИ ВАЖНО: UI Elements vs Board Content](#ui-elements)
3. [Правильный метод создания объектов](#создание-объектов)
4. [Принципы создания диаграмм (3C Framework)](#принципы)
5. [Системы дизайна и стилей](#дизайн)
6. [Типы диаграмм и их применение](#типы)
7. [Workflow для сложных диаграмм (20+ элементов)](#сложные-диаграммы)
8. [Координатная система и позиционирование](#координаты)
9. [Frames vs Shapes - критические различия](#frames)
10. [Оптимальный рабочий процесс](#рабочий-процесс)
11. [Работа с изображениями: преобразование в Miro](#преобразование)
12. [Практический пример: Архитектурная диаграмма](#пример)
13. [Troubleshooting - решение частых проблем](#troubleshooting)
14. [Производительность и оптимизация](#производительность)
15. [Чеклист качества](#чеклист)

---

## <a name="интерфейс"></a>1. Понимание интерфейса Miro

### Основные элементы интерфейса

```
┌──────────────────────────────────────────────────────────────┐
│ [≡] Miro Logo    [Board Name]    [Share] [☰] [...] [Profile]  │ ← Верхняя панель
├──────────┬────────────────────────────────────┬──────────────┤
│          │                                    │              │
│ Левая    │         Бесконечный холст          │   Правая     │
│ панель   │        (доска с объектами)        │   панель     │
│ инстру-  │                                    │   (контекст- │
│ ментов   │                                    │   ные меню)  │
│          │                                    │              │
│          │                                    │              │
├──────────┴────────────────────────────────────┴──────────────┤
│             [Zoom] [Frames] [Layers] [?]                      │ ← Нижняя панель
└──────────────────────────────────────────────────────────────┘
```

### Левая панель инструментов (Creation Toolbar)

**Основные инструменты (сверху вниз):**

| Горячая клавиша | Инструмент | Назначение |
|----------------|------------|------------|
| `V` | Select (Курсор) | Выбор и перемещение объектов |
| `H` | Hand (Рука) | Навигация по доске |
| `T` | Text | Создание текстовых блоков |
| `N` | Sticky Notes | Создание стикеров |
| `S` | Shapes | Открытие панели фигур |
| `R` | Rectangle | Быстрое создание прямоугольника |
| `O` | Oval | Быстрое создание круга |
| `L` | Connection Line | Создание соединительных линий |
| `D` | Card | Создание карточек |
| `P` | Pen | Рисование от руки |
| `E` | Eraser | Стирание нарисованного |
| `C` | Comment | Добавление комментария |
| `F` | Frame | Создание фрейма |

### Правая панель (Context Panel)

Появляется автоматически при выборе объекта. Содержит:
- **Свойства объекта**: цвет заливки, цвет границы, толщина линии
- **Форматирование текста**: шрифт, размер, выравнивание, жирный/курсив
- **Позиционирование**: координаты X/Y, ширина/высота
- **Специальные опции**: ссылки, замок, слои (z-index)

### Нижняя панель навигации

- **Zoom control**: уровень масштабирования (обычно в %)
- **Frames panel**: список всех фреймов на доске
- **Layers panel**: управление слоями (z-order)
- **Help (?)**: горячие клавиши и справка

### Контекстное меню объекта (Context Toolbar)

Появляется при выделении объекта **над выделенным элементом** или **сбоку от него**:
- Изменение формы (для фигур)
- Цвет заливки
- Цвет границы
- Блокировка
- Дополнительные опции (три точки `...`)

---

## <a name="ui-elements"></a>2. КРИТИЧЕСКИ ВАЖНО: UI Elements vs Board Content

### ⚠️ НЕ ПУТАЙТЕ: Что является частью интерфейса, а что — содержимым доски

Одна из самых распространённых ошибок — попытка "удалить" или "исправить" элементы, которые являются частью интерфейса Miro, а не доски.

#### 🔵 Голубая рамка вокруг объектов

```
┌─────────────┐
│  ○       ○  │  ← Голубая рамка с точками
│             │
│   Element   │
│             │
│  ○       ○  │
└─────────────┘
```

**ЧТО ЭТО:**
- Handles для изменения размера (resize handles)
- 8 точек на прямоугольнике: 4 угла + 4 середины сторон
- Значок поворота в нижнем левом углу

**КОГДА ПОЯВЛЯЮТСЯ:**
- Только когда элемент выбран (selected)

**КОГДА ИСЧЕЗАЮТ:**
- После нажатия `Escape`
- После клика в пустое место холста
- После выбора другого элемента

**ЧТО ДЕЛАТЬ:**
- ❌ НЕ ПЫТАЙТЕСЬ удалить эти точки
- ✅ Нажмите `Escape` для снятия выделения
- ✅ Это нормальное поведение интерфейса

**Уровень уверенности:** 100% — это стандартный UI-элемент

---

#### 🔵 Синие точки на краях объектов (Connection Points)

```
     ●  ← Синяя точка
┌─────────────┐
│             │●
│   Element   │
●│             │
└─────────────┘
     ●
```

**ЧТО ЭТО:**
- Connection points — точки для создания соединительных линий
- Появляются при наведении курсора на края выбранного элемента
- Обычно располагаются на всех 4 сторонах + в центре объекта

**КАК ИСПОЛЬЗОВАТЬ:**
1. Выберите объект (появится голубая рамка)
2. Наведите курсор на край объекта → появятся синие точки
3. Кликните на синюю точку и перетащите к другому объекту
4. Отпустите → создастся соединительная линия

**ЧТО ДЕЛАТЬ:**
- ❌ НЕ ПЫТАЙТЕСЬ удалить синие точки
- ✅ Используйте их для создания связей между объектами
- ✅ Если не нужны — просто нажмите `Escape` или кликните в сторону

---

#### 📐 Умные направляющие (Smart Guides)

```
┌──────┐ : : : : ┌──────┐
│ Box1 │ : : : : │ Box2 │
└──────┘ : : : : └──────┘
         ↑ Розовые/фиолетовые пунктирные линии
```

**ЧТО ЭТО:**
- Умные направляющие для выравнивания (smart guides / alignment guides)
- Показывают, когда объекты выровнены друг относительно друга
- Могут быть **розовыми/фиолетовыми** или **серыми**

**КОГДА ПОЯВЛЯЮТСЯ:**
- При перемещении объекта
- Когда край или центр объекта совпадает с другим объектом
- При включенной опции "Align objects" (Preferences → Align objects)

**ЧТО ДЕЛАТЬ:**
- ✅ Используйте их для точного позиционирования
- ✅ Они исчезают автоматически после завершения перемещения
- ✅ Они НЕ сохраняются в финальной доске
- ✅ Можно отключить в Preferences, если мешают

---

#### 🖱️ Плавающая панель инструментов (Floating Context Toolbar)

```
┌──────────────────────────────────────┐
│ [🔲] [🎨] [🔗] [🔒] [...]           │ ← Панель над объектом
└──────────────────────────────────────┘
         ↓
┌─────────────┐
│   Element   │
└─────────────┘
```

**ЧТО ЭТО:**
- Плавающая панель инструментов (floating context toolbar)
- Появляется **над** или **сбоку** от выделенного объекта
- Содержит быстрые действия: изменение формы, цвета, блокировка и т.д.

**КОГДА ПОЯВЛЯЕТСЯ:**
- Сразу после выбора объекта

**ПРОБЛЕМА:**
- Может закрывать connection points или другие элементы
- Может мешать при работе с плотно расположенными объектами

**ЧТО ДЕЛАТЬ:**
- ✅ Используйте её для быстрого доступа к настройкам
- ✅ Если мешает — **отодвиньте холст** (Space + drag) или **уменьшите зум**
- ✅ Альтернатива — используйте правую панель свойств
- ❌ К сожалению, нельзя переместить или скрыть эту панель вручную

---

### 📊 Сводная таблица: Интерфейс vs Содержимое доски

| Элемент | Цвет | Когда видно | Часть доски? | Действие |
|---------|------|-------------|--------------|----------|
| Resize handles (точки) | Голубой | Объект выбран | ❌ НЕТ | Нажать Escape |
| Connection points (синие точки) | Синий | При наведении на край | ❌ НЕТ | Использовать для соединений |
| Smart guides | Розовый/серый пунктир | При перемещении | ❌ НЕТ | Использовать для выравнивания |
| Floating toolbar | Серый/белый | Объект выбран | ❌ НЕТ | Использовать или игнорировать |
| Фигуры/текст | Любой | Всегда | ✅ ДА | Это ваша доска |
| Соединительные линии | Чёрный/цветной | Всегда | ✅ ДА | Это ваша доска |
| Frames (рамки) | Любой | Всегда | ✅ ДА | Это ваша доска |
| Стикеры | Жёлтый/цветной | Всегда | ✅ ДА | Это ваша доска |

---

## <a name="создание-объектов"></a>3. Правильный метод создания объектов

### ✅ РЕКОМЕНДУЕМЫЙ метод: Использование горячих клавиш

Miro оптимизирован для работы с **горячими клавишами**. Это самый быстрый и надёжный способ создания объектов.

#### Основные горячие клавиши для создания

```javascript
// ✅ ПРАВИЛЬНЫЙ МЕТОД

// 1. Создание прямоугольника
press("R")                      // Активировать инструмент Rectangle
drag_from(start_x, start_y)     // Начальная точка
drag_to(end_x, end_y)           // Конечная точка (размер фигуры)
type("Element Name")            // Сразу вводим текст (автофокус)
press("Escape")                 // Снять выделение

// 2. Создание круга
press("O")                      // Активировать инструмент Oval
drag_from(start_x, start_y)
drag_to(end_x, end_y)
type("Label")
press("Escape")

// 3. Создание стикера
press("N")                      // Активировать Sticky Notes
click(position_x, position_y)   // Клик в нужном месте
type("Text")                    // Ввод текста
press("Escape")

// 4. Создание текстового блока
press("T")                      // Активировать Text
click(position_x, position_y)
type("Text content")
press("Escape")

// 5. Создание соединительной линии
press("L")                      // Активировать Connection Line
drag_from(start_x, start_y)
drag_to(end_x, end_y)
press("Escape")
```

**Успешность:** 100%  
**Скорость:** ~2 секунды на элемент  
**Рекомендация:** ⭐⭐⭐⭐⭐ Используйте всегда

---

### ✅ Альтернативный метод: Использование левой панели инструментов

```javascript
// Метод для тех случаев, когда нужна специфическая фигура

// 1. Открыть панель фигур
press("S")                      // Или click на иконку Shapes в левой панели

// 2. Выбрать фигуру из списка
click(shape_icon)               // Например, треугольник, звезда, стрелка и т.д.

// 3. Нарисовать на холсте
drag_from(start_x, start_y)
drag_to(end_x, end_y)
type("Label")
press("Escape")
```

**Когда использовать:**
- Нужна специфическая фигура (звезда, треугольник, параллелограмм, облако и т.д.)
- Работаете с диаграммами, требующими стандартных нотаций (UML, BPMN и т.д.)

---

### 📍 Создание фигур с зажатым Shift (пропорциональность)

```javascript
// Создание идеального квадрата (вместо прямоугольника)
press("R")
press_and_hold("Shift")         // Зажать Shift
drag_from(start_x, start_y)
drag_to(end_x, end_y)           // Фигура будет пропорциональной
release("Shift")
type("Label")
press("Escape")

// Создание идеального круга (вместо овала)
press("O")
press_and_hold("Shift")
drag_from(start_x, start_y)
drag_to(end_x, end_y)
release("Shift")
type("Label")
press("Escape")
```

**Применение:**
- Когда нужны идеально пропорциональные фигуры
- Создание иконок, кнопок, маркеров

---

### 🚀 Оптимизация: Быстрое создание связанных объектов

```javascript
// Быстрый способ создания объектов с автоматическими связями

// 1. Выбрать существующий объект
click(existing_object)

// 2. Навести на синюю точку на краю → появится подсказка:
//    - Если рядом есть объект → предложит создать соединение
//    - Если рядом нет объектов → предложит создать такой же объект

// 3. Кликнуть на синюю точку
click(blue_connection_point)

// 4. Автоматически создастся:
//    - Новый объект того же типа (если рядом ничего нет)
//    - ИЛИ соединительная линия к ближайшему объекту

// 5. Ввести текст
type("New Element")
press("Escape")
```

**Преимущества:**
- Создаёт объект + соединение за одно действие
- Автоматически размещает объект на оптимальном расстоянии
- Ускоряет создание flowchart и process diagrams

**Ускорение:** В 2 раза быстрее, чем создание объекта + отдельное создание соединения

---

### 💡 Копирование объектов (самый быстрый способ)

```javascript
// Метод 1: Cmd/Ctrl + D (Duplicate)
click(object)                   // Выбрать объект
press("Cmd+D")                  // Mac: Cmd+D, Windows: Ctrl+D
// Копия создаётся с небольшим смещением

// Метод 2: Alt/Option + Drag
click(object)                   // Выбрать объект
press_and_hold("Alt")           // Windows: Alt, Mac: Option
drag_to(new_x, new_y)           // Перетащить → создаётся копия
release("Alt")

// Метод 3: Alt/Option + Arrow keys (копия со смещением по сетке)
click(object)
press("Alt+ArrowRight")         // Копия справа
// Или: Alt+ArrowLeft, Alt+ArrowUp, Alt+ArrowDown
```

**Применение:**
- Когда нужно создать несколько одинаковых объектов
- При создании повторяющихся элементов (серверы, компоненты и т.д.)

**Ускорение:** В 5 раз быстрее, чем создание с нуля + копирование стиля

---

### 📐 Специальные фигуры для диаграмм

Miro предоставляет **специализированные библиотеки фигур** для различных типов диаграмм:

#### Flowchart shapes (Фигуры для блок-схем)
```
- Process (Прямоугольник) — процесс
- Decision (Ромб) — условие
- Start/End (Скруглённый прямоугольник) — начало/конец
- Data (Параллелограмм) — ввод/вывод данных
- Document (Прямоугольник с волнистым низом) — документ
- Database (Цилиндр) — база данных
```

Доступ: `S` → выбрать "Flowchart" из списка

#### UML shapes (Фигуры UML)
```
- Class (Прямоугольник с секциями)
- Package (Прямоугольник с вкладкой)
- Actor (Человечек)
- Use Case (Овал)
```

Доступ: `S` → выбрать "UML" из списка

#### Network/Cloud shapes (Сетевые фигуры)
```
- Cloud (Облако)
- Server (Прямоугольник с разделителями)
- Router (Цилиндр с антеннами)
- Database (Цилиндр)
```

Доступ: `S` → выбрать "Network" или "Cloud" из списка

---

### ⚠️ ЧАСТЫЕ ОШИБКИ и их решения

#### ❌ Ошибка 1: Фигура не создаётся при клике
**Причина:** Инструмент не активирован  
**Решение:** Убедитесь, что нажали горячую клавишу (`R`, `O`, `S` и т.д.) или выбрали инструмент на панели

#### ❌ Ошибка 2: Текст не вводится в объект
**Причина:** Объект потерял фокус  
**Решение:** 
- После создания объекта сразу начинайте печатать (не кликайте в сторону!)
- Если фокус потерян — дважды кликните на объект для редактирования текста

#### ❌ Ошибка 3: Фигура получилась слишком маленькой/большой
**Причина:** Неправильно определили размер при drag  
**Решение:** 
- Измените размер, потянув за углы голубой рамки
- Или используйте правую панель для точного задания Width/Height

#### ❌ Ошибка 4: Не могу создать несколько объектов подряд
**Причина:** Забыли нажать `Escape` после создания  
**Решение:** 
- **Всегда** завершайте создание объекта нажатием `Escape`
- Это снимает выделение и готовит к следующему действию

---

## <a name="принципы"></a>4. Принципы создания диаграмм (3C Framework)

Профессиональные диаграммы в Miro следуют принципу **3C**:

### **Concise (Краткость)**

- ❌ Избегайте перегруженности информацией
- ✅ Один уровень абстракции на доску или фрейм
- ✅ Используйте **фреймы (Frames)** для разделения логических частей
- ✅ Применяйте **слои (Layers)** для управления сложностью

**Пример:** Вместо одной гигантской доски "Вся система" → создайте:
- Frame 1 (L1): Обзор высокого уровня
- Frame 2 (L2): Детальная архитектура бэкенда
- Frame 3 (L2): Детальная архитектура фронтенда
- Frame 4 (L3): Data flow диаграмма

**Преимущество фреймов:**
- Можно переключаться между ними через Frames panel (нижняя панель)
- Можно экспортировать каждый фрейм отдельно
- Можно презентовать по порядку (Presentation mode)

---

### **Clear (Ясность)**

- ✅ Правильный выбор типа диаграммы под задачу
- ✅ Осмысленные подписи (не "Блок 1", а "API Gateway")
- ✅ Логичное направление потоков:
  - **Слева направо** для процессов (процесс → результат)
  - **Сверху вниз** для иерархий (родитель → дочерний элемент)
- ✅ Достаточное расстояние между элементами (минимум 30-50px)

**Решение ветвления (decision points):**

```
[Действие] → ◇ Условие?
              ├─ Да → [Результат A]
              └─ Нет → [Результат B]
```

**Используйте цветовое кодирование:**
- Разные цвета = разные сущности/этапы/команды
- Одинаковые элементы = одинаковый цвет

---

### **Consistent (Согласованность)**

#### Цвета

- Используйте **ограниченную палитру** (3-6 основных цветов)
- Применяйте цвета для **группировки**, а не украшения
- Создайте **легенду** (цветовой ключ) в углу доски

**Рекомендованная палитра для IT-диаграмм:**

```
#FFD54F — Жёлтый (серверы приложений, VM, compute)
#FFB74D — Оранжевый (балансировщики, очереди, middleware)
#BBDEFB — Голубой (базы данных, data storage)
#81C784 — Зелёный (хранилища, file storage, CDN)
#F48FB1 — Розовый (обработка медиа, processing)
#B0BEC5 — Серый (клиенты, пользователи, external)
#FFF8E1 — Светло-жёлтый (фоновые контейнеры/фреймы)
#FFFFFF — Белый (нейтральные элементы)
```

#### Формы

**Стандартные соглашения (универсальные):**

| Форма | Назначение | Пример |
|-------|-----------|--------|
| **Прямоугольник со скруглёнными углами** | Процесс, сервис, компонент | "API Server", "Auth Service" |
| **Прямоугольник** | Контейнер, система, группа | "Backend", "Frontend" |
| **Ромб** | Условие, точка принятия решения | "User authenticated?" |
| **Параллелограмм** | Ввод/вывод данных | "User Input", "API Response" |
| **Круг** | Начало/конец процесса, событие | "Start", "End", "Event Triggered" |
| **Цилиндр** | База данных, data warehouse | "PostgreSQL", "MongoDB" |
| **Облако** | Внешний сервис, CDN, cloud | "AWS S3", "Cloudflare CDN" |
| **Шестиугольник** | Обработка, трансформация | "Data Processing", "Converter" |
| **Документ** | Файл, отчёт, документация | "Report", "Config File" |

**Стикеры (Sticky Notes):**
- Используйте для **временных заметок**, **комментариев** или **brainstorming**
- НЕ используйте в финальных диаграммах (выглядит непрофессионально)
- Исключение: Kanban-доски, ретроспективы, воркшопы

#### Соединители (Connection Lines)

**Типы линий:**

| Тип | Назначение | Применение |
|-----|-----------|------------|
| **Сплошная стрелка** `→` | Основной поток данных/процесса | A отправляет данные в B |
| **Пунктирная стрелка** `- - →` | Опциональный/асинхронный поток | Callback, optional dependency |
| **Толстая стрелка** `═══>` | Высокая нагрузка/приоритет | Основной трафик |
| **Двунаправленная** `↔` | Синхронное взаимодействие | Request-response |
| **Без стрелки** `——` | Просто связь (без направления) | Ассоциация между компонентами |

**Настройка в Miro:**
- Выбрать линию → Context Toolbar → выбрать тип стрелки (start/end)
- Изменить стиль линии: Solid / Dashed / Dotted
- Изменить толщину: тонкая (1-2px) / средняя (3-4px) / толстая (6-8px)

**Подписи на линиях:**
- Дважды кликнуть на линию → ввести текст
- Используйте для указания: протокол (HTTP, gRPC), формат данных (JSON, XML), тип события

---

## <a name="дизайн"></a>5. Системы дизайна и стилей

### Z-Order (Слои): Управление перекрытием объектов

В Miro, как и в любом графическом редакторе, объекты имеют **порядок наложения** (z-order). Это критически важно при работе с фреймами и группировками.

#### Горячие клавиши для управления слоями

**macOS:**
```
Cmd + Shift + ↑     — Bring to Front (на передний план)
Cmd + Shift + ↓     — Send to Back (на задний план)
Fn + ↑ (или PgUp)   — Bring Forward (на один слой вперёд)
Fn + ↓ (или PgDn)   — Send Backward (на один слой назад)
```

**Windows:**
```
Ctrl + Shift + PgUp    — Bring to Front
Ctrl + Shift + PgDn    — Send to Back
PgUp                   — Bring Forward
PgDn                   — Send Backward
```

#### Алгоритм работы с фреймами/контейнерами

```javascript
// ПРОБЛЕМА: Фрейм закрывает все элементы внутри

// РЕШЕНИЕ:

// 1. Сначала создайте все элементы внутри
press("R")
drag_from(300, 300), drag_to(400, 350)
type("Server 1")
press("Escape")

press("R")
drag_from(450, 300), drag_to(550, 350)
type("Server 2")
press("Escape")

// 2. Создайте фрейм вокруг них
press("F")  // Frame tool
drag_from(250, 250), drag_to(600, 400)  // Обводим элементы
double_click(frame_title_area)          // Редактировать заголовок фрейма
type("Backend Services")
press("Escape")

// 3. ВАЖНО: Фрейм НЕ НУЖНО отправлять назад!
// В Miro фреймы автоматически ведут себя как контейнеры,
// содержимое всегда видно поверх фрейма
```

**Ключевое отличие от draw.io:**
- ❌ В draw.io контейнеры нужно вручную отправлять на задний план
- ✅ В Miro фреймы (Frames) автоматически работают как контейнеры
- ✅ Содержимое фрейма всегда видно

**Когда нужно использовать Send to Back:**
- Когда используете **обычные фигуры** (не фреймы) в качестве фона/группировки
- Когда создаёте декоративные элементы (фоновые прямоугольники для визуального выделения)

Пример:
```javascript
// Создаём фоновый прямоугольник под группой элементов
press("R")
drag_from(200, 200), drag_to(700, 500)
// Выбрать цвет: светло-жёлтый #FFF8E1, прозрачность 50%
press("Cmd+Shift+↓")  // Send to Back — СРАЗУ!
press("Escape")

// Теперь создаём элементы поверх фона
// ...
```

---

### Управление Viewport и Zoom

#### Уровни зума и их применение

| Зум | Назначение | Когда использовать |
|-----|------------|-------------------|
| **100%** | Детальная работа | Создание элементов, ввод текста, точное позиционирование |
| **50-75%** | Средний обзор | **Проверка после каждых 5-10 элементов** |
| **25-40%** | Широкий обзор | Финальная проверка всей доски |
| **Fit to Screen** | Автомасштаб | Видеть всё содержимое доски |

#### Горячие клавиши для зума

**macOS:**
```
Cmd + +          — Zoom In (увеличить)
Cmd + -          — Zoom Out (уменьшить)
Cmd + 0          — Zoom to 100%
Option + 1       — Fit to Screen (вписать в окно)
Option + 2       — Zoom to Selection (увеличить выбранный объект)
```

**Windows:**
```
Ctrl + +         — Zoom In
Ctrl + -         — Zoom Out
Ctrl + 0         — Zoom to 100%
Alt + 1          — Fit to Screen
Alt + 2          — Zoom to Selection
```

#### Best Practice: Zoom Management

```
Начало работы:
├─ Установить 100% (Cmd/Ctrl + 0)
├─ Создать 5-10 элементов
├─ Переключиться на 50-75% → визуальная проверка
├─ Вернуться на 100%
└─ Повторить цикл

Финальная проверка:
├─ Fit to Screen (Option/Alt + 1)
├─ Проверить всю доску целиком
├─ Zoom to 50% для детального просмотра разных зон
└─ Исправить недочёты
```

---

### Навигация по холсту

**Перемещение холста:**
```
Space + Drag      — Перемещение холста (панорамирование)
Правая кнопка мыши + Drag  — Также перемещение холста
```

**Выделить все элементы (найти "потерянные"):**
```
Cmd + A (Mac) / Ctrl + A (Windows)  — Выделить всё
```
Результат: Все элементы получат голубую рамку — можно увидеть, где они расположены.

---

### Копирование размеров и стилей

#### Копирование стиля (Format Painter)

**Метод 1: Горячие клавиши**

**macOS:**
```
1. Выбрать объект с нужным стилем: click(source_object)
2. Копировать стиль: Cmd + Shift + C
3. Выбрать целевые объекты: click(target), shift_click(target2)...
4. Вставить стиль: Cmd + Shift + V
```

**Windows:**
```
1. click(source_object)
2. Ctrl + Shift + C
3. click(target), shift_click(target2)...
4. Ctrl + Shift + V
```

**Метод 2: Через Context Toolbar**
```
1. Выбрать объект с нужным стилем
2. В context toolbar найти иконку "Paint format" (кисточка)
3. Кликнуть на неё
4. Кликнуть на целевые объекты
```

**Что копируется:**
- Цвет заливки
- Цвет границы
- Толщина границы
- Шрифт и размер текста
- Прозрачность

**Что НЕ копируется:**
- Размер объекта
- Позиция
- Текстовое содержимое

---

#### Копирование размера

В Miro нет встроенной функции "Copy Size" как в draw.io, но есть обходные пути:

**Метод 1: Через правую панель свойств**
```javascript
// 1. Выбрать эталонный объект
click(reference_object)

// 2. Посмотреть в правой панели: Width = X, Height = Y

// 3. Выбрать целевой объект
click(target_object)

// 4. Вручную установить такие же Width и Height в правой панели
```

**Метод 2: Использовать дублирование и замену**
```javascript
// 1. Выбрать эталонный объект нужного размера
click(reference_object)

// 2. Дублировать: Cmd/Ctrl + D
// Копия будет того же размера

// 3. Изменить текст и переместить
double_click(duplicated_object)
type("New Label")
press("Escape")
drag(duplicated_object, new_x, new_y)
```

---

### Выравнивание и распределение

#### Выравнивание объектов

Miro имеет встроенные инструменты выравнивания. Они появляются в **context toolbar** при выборе нескольких объектов.

**Горизонтальное выравнивание:**
```javascript
// 1. Выбрать объекты, которые должны быть на одной высоте
click(element1)
shift_click(element2)
shift_click(element3)

// 2. В context toolbar выбрать:
// - Align Top (выровнять по верхнему краю)
// - Align Middle (выровнять по центру)
// - Align Bottom (выровнять по нижнему краю)
```

**Вертикальное выравнивание:**
```javascript
// 1. Выбрать объекты в столбце
click(element1)
shift_click(element2)
shift_click(element3)

// 2. В context toolbar выбрать:
// - Align Left (выровнять по левому краю)
// - Align Center (выровнять по центру)
// - Align Right (выровнять по правому краю)
```

**Равномерное распределение:**
```javascript
// Для горизонтального распределения:
select_multiple(horizontal_elements)
// Context toolbar → Distribute Horizontally

// Для вертикального распределения:
select_multiple(vertical_elements)
// Context toolbar → Distribute Vertically
```

---

### Snap to Grid и Smart Guides

**Включение сетки и привязки:**

```javascript
// Способ 1: Через меню
// Верхняя панель → три точки (...) → Preferences
// Включить:
// - Show grid (показать сетку)
// - Snap objects (привязка объектов к сетке и друг к другу)

// Способ 2: Горячая клавиша для сетки
press("G")  // Переключает видимость сетки
```

**Настройки сетки:**
- Grid type: Line grid / Dot grid / None
- Grid spacing: обычно 10px или 20px
- Snap to grid: автоматическая привязка при перемещении

**Когда использовать:**
- ✅ Включайте всегда при создании диаграмм (упрощает выравнивание)
- ✅ "Snap objects" помогает создавать аккуратные схемы
- ❌ Отключайте для свободного творческого рисования (brainstorming, sketches)

---

## <a name="типы"></a>6. Типы диаграмм и их применение

Miro поддерживает создание различных типов диаграмм. Для каждого типа есть свои best practices.

### Flowchart (Блок-схема)

**Применение:** Описание процессов, алгоритмов, workflows

**Основные элементы:**
```
Начало/Конец: Скруглённый прямоугольник (Oval)
Процесс: Прямоугольник (Rectangle)
Решение: Ромб (Diamond)
Данные: Параллелограмм
Документ: Document shape
```

**Шаблон:** Miro → Templates → Flowchart

**Best practices:**
- Поток сверху вниз или слева направо
- Все решения (ромбы) должны иметь два выхода: "Да" и "Нет"
- Используйте подписи на стрелках для указания условий

---

### System Architecture Diagram (Архитектурная диаграмма)

**Применение:** Визуализация IT-систем, микросервисов, инфраструктуры

**Основные элементы:**
```
Серверы/Сервисы: Прямоугольники (цветовое кодирование)
Базы данных: Цилиндры
Хранилища: Параллелограммы или облака
Внешние сервисы: Облака
Балансировщики/Очереди: Специальные иконки
```

**Best practices:**
- Используйте цветовое кодирование по типу компонента
- Группируйте связанные компоненты фреймами (Frontend, Backend, Data Layer)
- Указывайте протоколы на соединительных линиях (HTTP, gRPC, WebSocket)
- Используйте толщину линий для обозначения интенсивности трафика

---

### UML Diagrams (Диаграммы UML)

**Типы:**
- Class Diagram (диаграмма классов)
- Sequence Diagram (диаграмма последовательности)
- Use Case Diagram (диаграмма прецедентов)

**Доступ:**
- Press `S` → выбрать "UML" из библиотеки фигур
- Или использовать интеграцию с PlantUML (через приложения Miro)

**Best practices:**
- Следуйте стандартной нотации UML
- Используйте стикеры для комментариев (но не в финальной версии)
- Экспортируйте в PDF для документации

---

### Mind Map (Интеллект-карта)

**Применение:** Brainstorming, структурирование идей, планирование

**Структура:**
```
Центральная идея (центр)
  ├─ Основная ветка 1
  │   ├─ Подветка 1.1
  │   └─ Подветка 1.2
  ├─ Основная ветка 2
  └─ Основная ветка 3
```

**Шаблон:** Miro → Templates → Mind Map

**Best practices:**
- Центральный элемент — самый крупный и яркий
- Используйте цветовое кодирование для разных категорий
- Ветки должны расходиться радиально от центра
- Используйте изогнутые линии (curved connections) вместо прямых

---

### Kanban Board (Канбан-доска)

**Применение:** Управление задачами, agile-разработка, отслеживание прогресса

**Структура:**
```
Фреймы (колонки):
[To Do] | [In Progress] | [Review] | [Done]

Стикеры (задачи) внутри каждого фрейма
```

**Шаблон:** Miro → Templates → Kanban

**Best practices:**
- Используйте фреймы для колонок
- Стикеры для задач (можно присваивать владельцев через @mentions)
- Цветовое кодирование по приоритету или типу задачи
- Можно добавлять теги и метки

---

### Data Flow Diagram (Диаграмма потоков данных)

**Применение:** Визуализация движения данных в системе

**Основные элементы:**
```
Внешние сущности: Квадраты
Процессы: Круги или скруглённые прямоугольники
Хранилища данных: Параллельные линии (или цилиндры)
Потоки данных: Стрелки с подписями
```

**Best practices:**
- Показывайте направление потока данных стрелками
- Подписывайте, какие данные передаются
- Используйте разные цвета для разных типов данных

---

## <a name="сложные-диаграммы"></a>7. Workflow для сложных диаграмм (20+ элементов)

При создании диаграмм с большим количеством элементов (20+) важно следовать структурированному подходу.

### 🎯 Проверенный алгоритм (основан на опыте draw.io, адаптирован для Miro)

Этот workflow сокращает время создания диаграммы на **60-70%** по сравнению с хаотичным подходом.

---

#### Этап 1: Подготовка (2-3 минуты)

```javascript
// 1. Создать новую доску или очистить существующую
// Miro → Dashboard → Create new board
// Или на существующей доске: Cmd/Ctrl + A → Delete

// 2. Установить оптимальный зум
press("Cmd+0")  // Zoom to 100%

// 3. Включить сетку и привязку
press("G")  // Показать сетку
// Меню → Preferences → Snap objects: ON

// 4. (Опционально) Создать цветовую легенду в углу доски
// Например, в правом верхнем углу:
// [Серверы] — жёлтый
// [БД] — голубой
// [Storage] — зелёный
```

**Ментальная подготовка:**
- Посчитайте общее количество элементов в референсной схеме
- Разделите элементы на логические группы:
  - Верхний ряд (клиентская часть)
  - Центр (основная логика, серверы)
  - Нижний ряд (данные, хранилища)
  - Внешние сервисы (справа/слева)
- Определите, какие группы будут во фреймах

---

#### Этап 2: Массовое создание элементов (15-25 минут для ~25 элементов)

**Принцип:** Создавайте ВСЕ элементы БЕЗ остановки на коррекции.

```javascript
// Группа 1: Верхний ряд (клиенты) — 3 элемента

press("R")  // Rectangle
drag_from(100, 100), drag_to(200, 150)
type("Users")
press("Escape")

press("R")
drag_from(250, 100), drag_to(350, 150)
type("Web Browser")
press("Escape")

press("R")
drag_from(400, 100), drag_to(500, 150)
type("Mobile App")
press("Escape")

// Быстрая проверка: Option/Alt + 1 (Fit to Screen), визуально проверить, Cmd/Ctrl + 0 (вернуться на 100%)

// Группа 2: Средний ряд (серверы) — 5 элементов

press("R")
drag_from(150, 250), drag_to(250, 300)
type("Load Balancer")
press("Escape")

press("R")
drag_from(100, 400), drag_to(200, 450)
type("Web Server 1")
press("Escape")

press("R")
drag_from(250, 400), drag_to(350, 450)
type("Web Server 2")
press("Escape")

press("R")
drag_from(175, 550), drag_to(275, 600)
type("App Server")
press("Escape")

press("R")
drag_from(175, 680), drag_to(275, 730)
type("Cache")
press("Escape")

// Группа 3: Базы данных (цилиндры) — 2 элемента
// Используем специальную фигуру

press("S")  // Открыть Shapes
// Выбрать "Flowchart" → найти Cylinder (Database)
drag_from(100, 800), drag_to(180, 900)
type("PostgreSQL")
press("Escape")

press("S")
// Снова Cylinder
drag_from(220, 800), drag_to(300, 900)
type("Redis")
press("Escape")

// ... продолжать для всех групп ...
```

**Важно:**
- ❌ **НЕ останавливайтесь** на настройку цветов
- ❌ **НЕ создавайте** стрелки между элементами
- ❌ **НЕ исправляйте** точное позиционирование
- ✅ **ТОЛЬКО создание** элементов с текстом
- ✅ Проверка Option/Alt + 1 каждые 7-10 элементов

---

#### Этап 3: Добавление фреймов (3-5 минут)

```javascript
// Фрейм 1: "Frontend"
press("F")  // Frame tool
drag_from(50, 50), drag_to(550, 200)  // Обводим клиентские элементы
double_click(frame_title)
type("Frontend")
press("Escape")

// Фрейм НЕ НУЖНО отправлять назад — в Miro фреймы автоматически контейнеры!

// Фрейм 2: "Backend"
press("F")
drag_from(50, 220), drag_to(400, 650)
double_click(frame_title)
type("Backend Services")
press("Escape")

// Фрейм 3: "Data Layer"
press("F")
drag_from(50, 770), drag_to(350, 920)
double_click(frame_title)
type("Data Layer")
press("Escape")

// ... остальные фреймы ...
```

**Настройка фреймов:**
- Изменить цвет фона (опционально): выбрать фрейм → Context Toolbar → Fill color
- Сделать фон полупрозрачным: выбрать фрейм → правая панель → Opacity: 20-30%
- Заблокировать фрейм (чтобы не двигался): Cmd/Ctrl + Shift + L

---

#### Этап 4: Стилизация (7-12 минут)

**Подход 1: По группам (рекомендуется)**

```javascript
// 1. Выбрать все серверы (жёлтый цвет)
click(server1)
shift_click(server2)
shift_click(server3)

// 2. Применить цвет через Context Toolbar или правую панель
// Fill color: #FFD54F (жёлтый)
// Border color: #F57C00 (оранжевый)
// Border width: 2px

// 3. Повторить для других групп

// Базы данных (голубой #BBDEFB)
select_group(databases)
// Fill: #BBDEFB

// Storage (зелёный #81C784)
select_group(storage_elements)
// Fill: #81C784

// Клиенты (серый #B0BEC5)
select_group(clients)
// Fill: #B0BEC5
```

**Подход 2: Copy-Paste Style (альтернатива)**

```javascript
// 1. Настроить одну фигуру вручную
click(server1)
// В правой панели установить нужные цвета, границы

// 2. Копировать стиль
press("Cmd+Shift+C")  // Mac
// или press("Ctrl+Shift+C")  // Windows

// 3. Выбрать остальные серверы
click(server2)
shift_click(server3)

// 4. Вставить стиль
press("Cmd+Shift+V")  // Mac
// или press("Ctrl+Shift+V")  // Windows
```

---

#### Этап 5: Создание соединителей (10-15 минут)

**Метод 1: Через горячую клавишу L**

```javascript
press("L")  // Connection Line tool
drag_from(source_x, source_y)
drag_to(target_x, target_y)
press("Escape")
```

**Метод 2: Синие точки на объектах (быстрее!)**

```javascript
// 1. Выбрать исходный объект
click(source_element)

// 2. Навести на синюю точку на краю
hover(blue_connection_point)

// 3. Кликнуть и перетащить к целевому объекту
drag_from(blue_point)
drag_to(target_element)

// Соединение создано!
press("Escape")
```

**Стилизация соединителей:**

```javascript
// Выбрать соединитель (кликнуть на линию)
click(connector_line)

// Context Toolbar или правая панель:
// - Line style: Solid / Dashed / Dotted
// - Start arrow: None / Arrow / Circle / Diamond
// - End arrow: None / Arrow / Circle / Diamond
// - Line width: 1-2px (тонкая) / 3-4px (средняя) / 6-8px (толстая)
// - Line type: Straight / Curved / Orthogonal (угловая)
```

**Добавление текста на соединитель:**

```javascript
// Дважды кликнуть на линию
double_click(connector_line)

// Ввести текст (например, "HTTP", "gRPC", "JSON")
type("HTTP")

// Escape для завершения
press("Escape")
```

---

#### Этап 6: Выравнивание (5-8 минут)

```javascript
// Горизонтальное выравнивание элементов
click(element1)
shift_click(element2)
shift_click(element3)

// Context Toolbar → Align:
// - Top (выровнять по верху)
// - Middle (выровнять по центру)
// - Bottom (выровнять по низу)

// Вертикальное выравнивание
click(element1)
shift_click(element2)
shift_click(element3)

// Context Toolbar → Align:
// - Left (выровнять по левому краю)
// - Center (выровнять по центру)
// - Right (выровнять по правому краю)

// Равномерное распределение
select_group(horizontal_elements)
// Context Toolbar → Distribute Horizontally

select_group(vertical_elements)
// Context Toolbar → Distribute Vertically
```

---

#### Этап 7: Финальная проверка и экспорт (3-5 минут)

```javascript
// 1. Fit to Screen
press("Option+1")  // Mac
// или press("Alt+1")  // Windows

// 2. Визуально проверить:
//    - Все элементы присутствуют?
//    - Все стрелки корректны?
//    - Фреймы не перекрывают содержимое?
//    - Нет опечаток?
//    - Цвета согласованы?

// 3. Сохранить (автосохранение в Miro, но можно принудительно)
// Изменения сохраняются автоматически

// 4. Экспорт (опционально)
// Верхняя панель → три точки (...) → Export
// Выбрать:
// - Export as Image (PNG, JPG)
// - Export as PDF
// - Export current frame (если нужен только один фрейм)
```

---

### ⏱️ Временные метрики (адаптированные для Miro)

| Этап | Оптимизированное время | Неоптимизированное время |
|------|----------------------|--------------------------|
| Подготовка | 2-3 мин | 2-3 мин |
| Создание 25 элементов | 15-25 мин | 35-50 мин |
| Фреймы | 3-5 мин | 8-12 мин |
| Стилизация | 7-12 мин | 15-20 мин |
| Соединители | 10-15 мин | 20-30 мин |
| Выравнивание | 5-8 мин | 10-15 мин |
| Проверка | 3-5 мин | 5-10 мин |
| **ИТОГО** | **45-73 мин** | **95-140 мин** |

**Ускорение: 2x (примерно в 2 раза быстрее)**

---

## <a name="координаты"></a>8. Координатная система и позиционирование

### Координатная система Miro

Miro использует **бесконечный холст** (infinite canvas), координаты могут быть как положительными, так и отрицательными.

```
        Y (вверх)
        ↑
        │
        │
────────┼───────→ X (вправо)
        │      (0,0)
        │
        ↓
```

**Центр доски:** (0, 0)

**Особенности:**
- В отличие от классических графических редакторов, Y растёт **вверх**, а не вниз
- Объекты могут быть созданы в любой точке (даже за пределами видимой области)
- Позиция объекта определяется координатами его **центра** (не левого верхнего угла!)

### Точное позиционирование через правую панель

```javascript
// Выбрать объект
click(object)

// В правой панели Properties:
// - X: координата центра по горизонтали
// - Y: координата центра по вертикали
// - W (Width): ширина
// - H (Height): высота
// - Rotation: угол поворота (в градусах)

// Установить точные значения вручную
set_property("X", 500)
set_property("Y", 300)
set_property("W", 150)
set_property("H", 80)
```

### Перемещение объектов с точностью до пикселя

```javascript
// Перемещение стрелками (шаг 10px при включённой сетке)
click(object)
press("ArrowRight")  // Вправо на 10px
press("ArrowLeft")   // Влево на 10px
press("ArrowUp")     // Вверх на 10px
press("ArrowDown")   // Вниз на 10px

// Точное перемещение (шаг 1px)
click(object)
press("Shift+ArrowRight")  // Вправо на 1px
press("Shift+ArrowLeft")   // Влево на 1px
press("Shift+ArrowUp")     // Вверх на 1px
press("Shift+ArrowDown")   // Вниз на 1px
```

---

## <a name="frames"></a>9. Frames vs Shapes - критические различия

### Что такое Frames в Miro?

**Frames (Фреймы)** — это специальные контейнеры для группировки содержимого на доске. Они отличаются от обычных фигур (Shapes).

| Характеристика | Frame (Фрейм) | Shape (Фигура) |
|---------------|---------------|----------------|
| Назначение | Контейнер, группировка | Визуальный элемент |
| Заголовок | Да (название фрейма) | Нет (только текст внутри) |
| Z-order | Автоматически на заднем плане | Может быть на любом слое |
| Содержимое | Может содержать другие объекты | Не может содержать |
| Навигация | Отображается в Frames panel | Не отображается |
| Презентация | Можно презентовать по порядку | Нельзя |
| Экспорт | Можно экспортировать отдельно | Экспортируется как часть доски |

### Когда использовать Frames

✅ **Используйте Frames для:**
- Группировки логических частей диаграммы (Frontend, Backend, Data Layer)
- Создания многостраничных диаграмм (каждый фрейм = страница)
- Презентаций (можно переключаться между фреймами)
- Экспорта отдельных частей диаграммы

❌ **НЕ используйте Frames для:**
- Простого визуального выделения (используйте обычный Rectangle)
- Маленьких группировок (2-3 объекта) — проще использовать Group (Cmd/Ctrl + G)

### Как создать Frame

```javascript
// Способ 1: Горячая клавиша
press("F")
drag_from(start_x, start_y)
drag_to(end_x, end_y)
double_click(frame_title_area)
type("Frame Name")
press("Escape")

// Способ 2: Обвести существующие объекты фреймом
select_multiple(objects)  // Shift + Click на каждый объект
right_click(selection)
// Контекстное меню → Create Frame

// Способ 3: Из левой панели
click(frame_icon_in_toolbar)
drag_from(start_x, start_y)
drag_to(end_x, end_y)
```

### Настройка Frames

```javascript
// Изменить размер фрейма
click(frame)
drag(resize_handle, new_position)

// Изменить цвет фона
click(frame)
// Context Toolbar → Fill color

// Сделать фон полупрозрачным
click(frame)
// Правая панель → Opacity: 20-50%

// Блокировать фрейм (чтобы не двигался случайно)
click(frame)
press("Cmd+Shift+L")  // Mac
// или press("Ctrl+Shift+L")  // Windows

// Разблокировать
press("Cmd+Shift+L")  // повторно
```

### Навигация по Frames

```javascript
// Открыть Frames panel
// Нижняя панель → иконка "Frames"

// В панели отображаются все фреймы на доске
// Клик на фрейм → холст переместится к этому фрейму и увеличит его

// Презентация фреймов
// Frames panel → кнопка "Present" (значок проектора)
// Фреймы будут показываться по порядку, как слайды
```

### Группировка фреймов

Можно группировать несколько фреймов вместе:

```javascript
// Выбрать несколько фреймов
click(frame1)
shift_click(frame2)
shift_click(frame3)

// Сгруппировать
press("Cmd+G")  // Mac
// или press("Ctrl+G")  // Windows

// Теперь фреймы перемещаются вместе
```

---

## <a name="рабочий-процесс"></a>10. Оптимальный рабочий процесс

См. раздел 7 "Workflow для сложных диаграмм" выше — там подробный алгоритм.

**Краткая сводка:**

1. ✅ **Подготовка** (2-3 мин): очистка доски, зум 100%, включить сетку
2. ✅ **Массовое создание** (15-25 мин): все элементы БЕЗ стилизации
3. ✅ **Фреймы** (3-5 мин): группировка логических частей
4. ✅ **Стилизация** (7-12 мин): цвета по группам
5. ✅ **Соединители** (10-15 мин): связи между элементами
6. ✅ **Выравнивание** (5-8 мин): Align + Distribute
7. ✅ **Проверка** (3-5 мин): Fit to Screen, визуальная проверка

**Золотое правило:** Делайте этапы последовательно, не перепрыгивайте между ними!

---

## <a name="преобразование"></a>11. Работа с изображениями: преобразование в Miro

### Алгоритм анализа изображения схемы

```
ВХОД: Изображение схемы (например, architecture-diagram.jpg)

ЭТАП 1: ВИЗУАЛЬНЫЙ АНАЛИЗ
├─ Подсчитать общее количество элементов
├─ Определить типы элементов:
│   ├─ Прямоугольники (скруглённые/обычные)
│   ├─ Цилиндры (базы данных)
│   ├─ Параллелограммы (storage)
│   ├─ Облака (CDN, cloud)
│   ├─ Специальные фигуры
│   └─ Фреймы/группировки
├─ Определить цветовую схему:
│   ├─ Жёлтый → серверы, VM
│   ├─ Голубой → базы данных
│   ├─ Зелёный → хранилища
│   ├─ Оранжевый → очереди, балансировщики
│   └─ Серый → клиенты
├─ Определить связи (стрелки):
│   ├─ Сплошные/пунктирные
│   ├─ Односторонние/двусторонние
│   └─ Толстые/тонкие
└─ Определить текст на элементах и соединителях

ЭТАП 2: СОСТАВЛЕНИЕ СПИСКА ЭЛЕМЕНТОВ
Создать структурированный список:
[
  {
    id: 1,
    type: "rectangle",
    label: "Users",
    color: "#B0BEC5",
    position: {x: 100, y: 100},
    size: {width: 100, height: 50}
  },
  {
    id: 2,
    type: "rectangle",
    label: "Web Browser",
    color: "#FFFFFF",
    position: {x: 250, y: 100},
    size: {width: 130, height: 60}
  },
  // ... остальные элементы ...
]

ЭТАП 3: СОСТАВЛЕНИЕ СПИСКА СОЕДИНИТЕЛЕЙ
[
  {
    from: 1,        // Users
    to: 2,          // Web Browser
    label: "",
    style: "solid",
    arrow: "single"
  },
  {
    from: 2,        // Web Browser
    to: 4,          // DNS
    label: "DNS query",
    style: "solid",
    arrow: "single"
  },
  // ... остальные связи ...
]

ЭТАП 4: СОСТАВЛЕНИЕ СПИСКА ФРЕЙМОВ
[
  {
    label: "Frontend",
    contains: [element_id_1, element_id_2],
    position: {x: 50, y: 50},
    size: {width: 500, height: 200}
  },
  // ... остальные фреймы ...
]

ЭТАП 5: СОЗДАНИЕ В MIRO
Следовать алгоритму из раздела 7 "Workflow для сложных диаграмм":
1. Подготовка доски
2. Массовое создание всех элементов
3. Добавление фреймов
4. Стилизация по группам
5. Создание соединителей
6. Выравнивание
7. Финальная проверка

ВЫХОД: Интерактивная доска Miro
```

---

## <a name="пример"></a>12. Практический пример: Архитектурная диаграмма

См. раздел 7 "Workflow для сложных диаграмм" — там есть пошаговый пример создания архитектурной диаграммы с ~25 элементами.

---

## <a name="troubleshooting"></a>13. Troubleshooting - решение частых проблем

### ❌ Проблема 1: "Не могу найти созданный объект"

**Причина:** Объект создан за пределами видимой области

**Решение:**
```javascript
// Выделить всё
press("Cmd+A")  // Mac
// или press("Ctrl+A")  // Windows

// Все объекты покажутся с голубыми рамками
// Можно увидеть, где они расположены

// Переместить холст к нужному объекту
// Или: Option/Alt + 1 (Fit to Screen)
```

---

### ❌ Проблема 2: "Фрейм закрывает содержимое"

**Причина:** Вы используете обычную фигуру (Rectangle) вместо Frame

**Решение:**
```javascript
// Если уже создали Rectangle:
click(rectangle)
press("Delete")

// Создать Frame вместо него
press("F")
drag_from(start_x, start_y)
drag_to(end_x, end_y)

// Фреймы автоматически ведут себя как контейнеры
```

---

### ❌ Проблема 3: "Не могу выделить объект за другим объектом"

**Причина:** Объект перекрыт другим объектом

**Решение:**
```javascript
// Способ 1: Временно переместить верхний объект
drag(top_object, temporary_position)
click(hidden_object)
drag(top_object, original_position)

// Способ 2: Отправить верхний объект назад
click(top_object)
press("Cmd+Shift+↓")  // Mac: Send to Back
// или press("Ctrl+Shift+PgDn")  // Windows

// Способ 3: Использовать Cmd/Ctrl + Click для множественного выбора
press_and_hold("Cmd")  // Mac (или Ctrl на Windows)
click(object1)
click(object2)  // Выделяются оба, даже если перекрываются
```

---

### ❌ Проблема 4: "Соединительная линия не прилипает к объекту"

**Причина:** Отключена привязка объектов (Snap)

**Решение:**
```javascript
// Включить привязку
// Меню → Preferences → Snap objects: ON

// Или: при создании линии наведите на край объекта
// Должна появиться синяя точка — кликните на неё
```

---

### ❌ Проблема 5: "Floating Toolbar мешает работе"

**Причина:** Toolbar появляется над connection points

**Решение (обходные пути):**
```javascript
// 1. Отодвинуть холст (панорамирование)
press_and_hold("Space")
drag(canvas, direction)

// 2. Уменьшить зум
press("Cmd+-")  // Mac
// или press("Ctrl+-")  // Windows

// 3. Использовать правую панель Properties вместо Toolbar
click(object)
// Правая панель → все те же опции

// К сожалению, скрыть Floating Toolbar нельзя
```

---

### ❌ Проблема 6: "Объекты не выравниваются по сетке"

**Причина:** Отключена привязка к сетке

**Решение:**
```javascript
// Включить сетку
press("G")  // Toggle grid

// Включить привязку
// Меню → Preferences → Snap objects: ON

// Теперь объекты будут "магнититься" к сетке
```

---

### ❌ Проблема 7: "Текст не помещается в фигуру"

**Причина:** Фигура слишком маленькая или текст слишком большой

**Решение:**
```javascript
// Способ 1: Увеличить размер фигуры
click(shape)
drag(resize_handle, new_size)

// Способ 2: Уменьшить размер шрифта
click(shape)
double_click(shape)  // Редактировать текст
select_all_text()
// Правая панель → Font size: уменьшить

// Способ 3: Перенести текст на несколько строк
double_click(shape)
// Вставить перенос строки: Enter
type("Line 1\nLine 2")
```

---

### ❌ Проблема 8: "Стиль не копируется на все объекты"

**Причина:** Не все объекты выделены

**Решение:**
```javascript
// Убедитесь, что выделили все нужные объекты
click(source_object)
press("Cmd+Shift+C")  // Копировать стиль

// Выделить ВСЕ целевые объекты через Shift+Click
click(target1)
shift_click(target2)
shift_click(target3)
// ... и т.д.

press("Cmd+Shift+V")  // Вставить стиль

// Или: используйте рамку выделения (drag selection)
drag_selection_box(start, end)  // Выделит все объекты в области
press("Cmd+Shift+V")
```

---

## <a name="производительность"></a>14. Производительность и оптимизация

### Горячие клавиши — ваше главное оружие

Использование горячих клавиш ускоряет работу в **3-5 раз**.

**Must-know горячие клавиши:**

| Действие | Mac | Windows |
|---------|-----|---------|
| **Select** | V | V |
| **Rectangle** | R | R |
| **Oval** | O | O |
| **Connection Line** | L | L |
| **Sticky Note** | N | N |
| **Frame** | F | F |
| **Text** | T | T |
| **Duplicate** | Cmd + D | Ctrl + D |
| **Copy Style** | Cmd + Shift + C | Ctrl + Shift + C |
| **Paste Style** | Cmd + Shift + V | Ctrl + Shift + V |
| **Group** | Cmd + G | Ctrl + G |
| **Ungroup** | Cmd + Shift + G | Ctrl + Shift + G |
| **Lock/Unlock** | Cmd + Shift + L | Ctrl + Shift + L |
| **Send to Back** | Cmd + Shift + ↓ | Ctrl + Shift + PgDn |
| **Bring to Front** | Cmd + Shift + ↑ | Ctrl + Shift + PgUp |
| **Undo** | Cmd + Z | Ctrl + Z |
| **Redo** | Cmd + Shift + Z | Ctrl + Shift + Z |
| **Select All** | Cmd + A | Ctrl + A |
| **Fit to Screen** | Option + 1 | Alt + 1 |
| **Zoom to Selection** | Option + 2 | Alt + 2 |
| **Zoom to 100%** | Cmd + 0 | Ctrl + 0 |
| **Toggle Grid** | G | G |
| **Deselect** | Esc | Esc |

---

### Оптимизация работы с большими досками

**1. Используйте Frames для разделения**
- Не создавайте гигантскую доску с 100+ объектами
- Разделите на фреймы по 15-25 объектов каждый
- Это ускоряет навигацию и экспорт

**2. Используйте Layers (слои) для управления сложностью**
- Нижняя панель → Layers
- Можно скрывать/показывать группы объектов
- Полезно для многоуровневых диаграмм

**3. Блокируйте завершённые части**
- Выделите группу → Cmd/Ctrl + Shift + L (Lock)
- Заблокированные объекты не двигаются случайно
- Ускоряет работу с остальной частью доски

**4. Используйте дублирование вместо создания с нуля**
- Cmd/Ctrl + D или Alt/Option + Drag
- В 5 раз быстрее, чем создание + стилизация

**5. Регулярно используйте Fit to Screen**
- Option/Alt + 1
- Проверка прогресса каждые 10-15 минут

---

### Избегайте этих распространённых ошибок

| ❌ Ошибка | ✅ Правильно |
|----------|-------------|
| Создавать объекты по одному с полной стилизацией | Создать все объекты, потом стилизовать группами |
| Использовать мышь для всего | Использовать горячие клавиши |
| Работать на зуме 100% постоянно | Регулярно переключаться на Fit to Screen |
| Использовать обычные фигуры для группировки | Использовать Frames |
| Не использовать сетку и привязку | Всегда включать Grid + Snap |
| Создавать соединения вручную через меню | Использовать синие точки на объектах |
| Стилизовать объекты по одному | Copy/Paste Style для групп |

---

## <a name="чеклист"></a>15. Чеклист качества диаграммы

### ✅ Перед завершением проверьте:

#### Содержание
- [ ] Все элементы из описания/изображения присутствуют
- [ ] Все связи между элементами отображены
- [ ] Направления стрелок корректны
- [ ] Labels на элементах информативны и кратки
- [ ] Labels на соединителях (где необходимо) присутствуют

#### Визуальная согласованность
- [ ] Одинаковые элементы имеют одинаковую форму
- [ ] Одинаковые элементы имеют одинаковый размер (или пропорциональны)
- [ ] Цветовая схема последовательна (не более 6 основных цветов)
- [ ] Используются стандартные обозначения (для flowchart, UML и т.д.)

#### UI Elements (критично!)
- [ ] Нажат Escape после каждого элемента (нет голубых рамок)
- [ ] Нет попыток "удалить" UI-элементы (синие точки, направляющие)
- [ ] Фреймы используются правильно (не обычные фигуры)

#### Читаемость
- [ ] Элементы не перекрываются
- [ ] Достаточное расстояние между элементами (минимум 30-50px)
- [ ] Текст читаем (размер шрифта минимум 12-14px)
- [ ] Соединители не пересекаются без необходимости

#### Структура
- [ ] Логичное направление потока (сверху вниз или слева направо)
- [ ] Группировки обозначены (фреймы, цвет, расположение)
- [ ] Иерархия визуально очевидна

#### Профессионализм
- [ ] Выравнивание элементов (Align)
- [ ] Равномерное распределение (Distribute)
- [ ] Сетка соблюдена (если включена)
- [ ] Нет орфографических ошибок

#### Экспорт и доступность
- [ ] Доска сохранена (автосохранение в Miro, но проверьте)
- [ ] Имя доски осмысленное
- [ ] Если нужно экспортировать — выбран правильный формат (PNG/PDF)
- [ ] Если нужен доступ — Share настроен правильно

---

## 🚀 Краткая справка: Горячие клавиши

### Создание объектов

| Действие | Горячая клавиша |
|---------|----------------|
| Select (Курсор) | V |
| Hand (Рука, навигация) | H |
| Rectangle | R |
| Oval | O |
| Connection Line | L |
| Text | T |
| Sticky Note | N |
| Shapes (открыть панель) | S |
| Frame | F |
| Pen (рисование) | P |
| Eraser | E |
| Comment | C |

### Редактирование

| Действие | Mac | Windows |
|---------|-----|---------|
| Дублировать | Cmd + D | Ctrl + D |
| Копировать | Cmd + C | Ctrl + C |
| Вставить | Cmd + V | Ctrl + V |
| Вырезать | Cmd + X | Ctrl + X |
| Удалить | Delete | Backspace |
| Отменить | Cmd + Z | Ctrl + Z |
| Повторить | Cmd + Shift + Z | Ctrl + Shift + Z |
| Выделить всё | Cmd + A | Ctrl + A |
| Снять выделение | Esc | Esc |

### Группировка и слои

| Действие | Mac | Windows |
|---------|-----|---------|
| Группировать | Cmd + G | Ctrl + G |
| Разгруппировать | Cmd + Shift + G | Ctrl + Shift + G |
| Заблокировать | Cmd + Shift + L | Ctrl + Shift + L |
| На передний план | Cmd + Shift + ↑ | Ctrl + Shift + PgUp |
| На задний план | Cmd + Shift + ↓ | Ctrl + Shift + PgDn |
| На слой вперёд | Fn + ↑ или PgUp | PgUp |
| На слой назад | Fn + ↓ или PgDn | PgDn |

### Стилизация

| Действие | Mac | Windows |
|---------|-----|---------|
| Копировать стиль | Cmd + Shift + C | Ctrl + Shift + C |
| Вставить стиль | Cmd + Shift + V | Ctrl + Shift + V |
| Жирный текст | Cmd + B | Ctrl + B |
| Курсив | Cmd + I | Ctrl + I |
| Подчёркнутый | Cmd + U | Ctrl + U |

### Навигация и зум

| Действие | Mac | Windows |
|---------|-----|---------|
| Увеличить | Cmd + + | Ctrl + + |
| Уменьшить | Cmd + - | Ctrl + - |
| Зум 100% | Cmd + 0 | Ctrl + 0 |
| Вписать в экран | Option + 1 | Alt + 1 |
| Зум на выделение | Option + 2 | Alt + 2 |
| Переключить сетку | G | G |
| Перемещение холста | Space + Drag | Space + Drag |

---

## 📌 Сравнение Miro vs draw.io

| Характеристика | Miro | draw.io |
|---------------|------|---------|
| **Тип** | Онлайн-доска (infinite canvas) | Редактор диаграмм |
| **Интерфейс** | Современный, минималистичный | Классический, плотный |
| **Создание фигур** | Горячие клавиши + drag | Drag-and-drop из панели |
| **Контейнеры** | Frames (автоматические) | Rectangles (нужно Send to Back) |
| **Соединители** | Синие точки на объектах | Оранжевый ромб или панель |
| **Collaboration** | Real-time, встроенная | Ограниченная |
| **Библиотеки фигур** | Меньше, но достаточно | Очень большие |
| **Presentation mode** | Да (через Frames) | Нет |
| **Экспорт** | PNG, PDF, JPG | PNG, SVG, PDF, XML, HTML |
| **Цена** | Freemium (ограничения на досках) | Бесплатно (open-source) |

---

## 💡 Частые вопросы (FAQ)

**Q: Можно ли использовать Miro без регистрации?**  
A: Нет, для создания досок нужна регистрация. Есть бесплатный тариф (Free plan) с ограничением в 3 редактируемые доски.

**Q: Как поделиться доской с коллегами?**  
A: Верхняя панель → кнопка "Share" → выбрать:
- Copy link (ссылка для просмотра)
- Invite to board (пригласить для редактирования)
- Настроить уровень доступа: View only / Can comment / Can edit

**Q: Можно ли работать офлайн?**  
A: Нет, Miro — полностью облачный сервис. Требуется интернет-соединение.

**Q: Как импортировать диаграммы из других инструментов?**  
A: Miro поддерживает импорт из:
- Lucidchart
- Microsoft Visio
- draw.io (через приложение)
- Можно импортировать изображения (PNG, JPG) и обводить вручную

**Q: Есть ли мобильное приложение?**  
A: Да, есть приложения для iOS и Android. Функционал ограничен по сравнению с веб-версией, но базовые операции доступны.

**Q: Как экспортировать доску?**  
A: Верхняя панель → три точки (...) → Export:
- Export as Image (вся доска или выбранная область)
- Export as PDF
- Export current frame (если работаете с фреймами)

**Q: Сохраняются ли изменения автоматически?**  
A: Да, Miro автоматически сохраняет все изменения в реальном времени. Нет кнопки "Сохранить".

---

## 🎓 Заключение

### Ключевые принципы успеха в Miro:

1. **Используйте горячие клавиши**: R, O, L, F, N, T — ваши лучшие друзья
2. **Frames — для группировки**, не обычные фигуры
3. **Всегда Escape**: после каждого элемента нажимайте Escape
4. **Синие точки**: используйте для быстрого создания соединений
5. **Работайте этапами**: создание → фреймы → стилизация → соединители → выравнивание
6. **Fit to Screen регулярно**: Option/Alt + 1 каждые 10-15 минут
7. **Copy/Paste Style**: стилизуйте группами, а не по одному
8. **Grid + Snap**: всегда включены для профессиональных диаграмм

### Производительность:

- **Неоптимизированный подход:** 95-140 минут на диаграмму из 25 элементов
- **Оптимизированный подход:** 45-73 минуты
- **Ускорение: 2x (примерно в 2 раза быстрее)**

### Золотые правила создания диаграмм в Miro:

1. ✅ **Планирование перед действием**: 3 минуты планирования экономят 30 минут исправлений
2. ✅ **Этапность работы**: не прыгайте между этапами
3. ✅ **Frames для структуры**: разделяйте сложные доски на фреймы
4. ✅ **Цветовое кодирование**: используйте 3-6 цветов с чёткой логикой
5. ✅ **Regular checkpoints**: Fit to Screen после каждых 7-10 элементов
6. ✅ **Горячие клавиши**: в 3-5 раз быстрее, чем мышь
7. ✅ **Copy/Paste для повторяющихся элементов**: дублирование вместо создания

### Анти-паттерны (чего избегать):

1. ❌ Создавать элементы в произвольном порядке
2. ❌ Стилизовать каждый элемент по отдельности
3. ❌ Использовать обычные фигуры вместо Frames для группировки
4. ❌ Работать без Grid и Snap
5. ❌ Забывать про Escape после создания элемента
6. ❌ Использовать только мышь (игнорировать горячие клавиши)
7. ❌ Создавать одну гигантскую доску вместо разделения на фреймы

---

## 📚 Дополнительные ресурсы

**Официальная документация:**
- https://help.miro.com/ — Help Center с подробными гайдами
- https://miro.com/templates/ — Библиотека шаблонов

**Полезные ссылки:**
- Shortcuts: нажмите `F1` на доске или `?` (Help) → Keyboard shortcuts
- YouTube канал Miro: множество video tutorials

---

**Удачи в создании диаграмм в Miro! 🎨📊**

---

## Лицензия и использование

Эта инструкция разработана для использования Ассистентом Perplexity в браузере Comet. Создана по аналогии с инструкциями для draw.io, адаптирована под особенности Miro.

**Версия:** 1.0  
**Дата:** 2025-11-22  
**Автор:** На основе структуры draw.io-instructions-v3.md, адаптировано для Miro